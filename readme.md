1.Создать окружение питон
1.1 Вариант через anaconda-  conda create -n 180126 python=3.12 -y
1.2. вариант через python - python -m venv 180126

2.активировать чистое окружение 
2.1. Anaconda - Conda активировать 180126 
2.2. Python — исходный 180126/bin/activate

3.Закинуть модели в директории  KNN в ml_models c моделями

4.выполнить: pip freeze -> requirements.txt (для сохранения версионности библиотек) Если надо установить, то выполняем pip install -r requirements.txt (на чистое окружение, чтобы не было конфликктов версий)